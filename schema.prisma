// This file is automatically generated by Keystone, do not modify it manually.
// Modify your Keystone config when you want to change this.

datasource postgresql {
  url               = env("DATABASE_URL")
  shadowDatabaseUrl = env("SHADOW_DATABASE_URL")
  provider          = "postgresql"
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id        String    @id @default(uuid()) @postgresql.Uuid
  name      String    @default("")
  email     String    @unique @default("")
  password  String
  todos     Task[]    @relation("Task_assignedTo")
  createdAt DateTime? @default(now())
  isAdmin   Boolean   @default(false)
}

model Task {
  id           String    @id @default(uuid()) @postgresql.Uuid
  label        String    @default("")
  description  String    @default("")
  assignedTo   User?     @relation("Task_assignedTo", fields: [assignedToId], references: [id])
  assignedToId String?   @map("assignedTo") @postgresql.Uuid
  followup     FollowUp? @relation("FollowUp_task")
  createdAt    DateTime? @default(now())
  completedAt  DateTime?

  @@index([assignedToId])
}

model FollowUp {
  id          String    @id @default(uuid()) @postgresql.Uuid
  label       String    @default("")
  description String    @default("")
  task        Task?     @relation("FollowUp_task", fields: [taskId], references: [id])
  taskId      String?   @unique @map("task") @postgresql.Uuid
  completedAt DateTime?
}
